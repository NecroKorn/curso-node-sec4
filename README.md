# Notas:

Primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de latbla de multiplicar         [number] [required]
  -l, --listar   Muestra la tabla de los cambios      [boolean] [default: false]
  -h, --hasta    Es hasta donde se calcula la tabla       [number] [default: 10]
```